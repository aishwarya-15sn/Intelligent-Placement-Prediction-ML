# Trained Models

This folder will contain trained machine learning models generated during the project development such as Logistic Regression, Random Forest, SVM, and XGBoost models used for placement prediction.
